/**
  * 
  * @param S string字符串 
  * @param T string字符串 
  * @return string字符串
  */
function minWindow(S, T) {
  let left = 0
  let right = 0
  let index = 0
  let result = ''

  for (let i = 0; i < S.length; i++) {

  }

  return result
}
module.exports = {
  minWindow: minWindow
};