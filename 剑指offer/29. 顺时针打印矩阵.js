/**
 * @param {number[][]} matrix
 * @return {number[]}
 */
var spiralOrder = function (matrix) {
  const res = []
  if (matrix.length === 0) return res
  const dir = [[0, 1], [1, 0], [0, -1], [-1, 0]]


  return res
};