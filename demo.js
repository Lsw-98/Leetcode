function demo(arr) {
  const dp = new Array(arr.length).fill(0)
  const res = []
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] <= 0) continue

  }
}

console.log(demo([9, 9, 6, 0, 6, 6, 9]));